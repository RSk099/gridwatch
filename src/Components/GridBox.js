import { Card } from "@mui/material";
import { styled as styledComponent } from "styled-components";

const GridBox = styledComponent(Card)`
  margin-bottom: 1rem;
  // width: 25rem;
`;

export default GridBox;
